apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pktgen-chart.fullname" . }}-test-connection"
  labels:
    {{- include "pktgen-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pktgen-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
