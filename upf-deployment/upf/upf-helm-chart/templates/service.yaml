apiVersion: v1
kind: Service
metadata:
  name: {{ include "upf-helm-chart.fullname" . }}
  labels:
    {{- include "upf-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "upf-helm-chart.selectorLabels" . | nindent 4 }}
